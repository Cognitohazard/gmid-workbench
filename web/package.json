{
  "name": "gmid-web",
  "private": true,
  "version": "0.1.0",
  "description": "gm/ID Workbench web UI: Svelte 5 + uPlot front end over @gmid/mostab-core; builds a hosted static site and a single self-contained offline HTML file.",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@gmid/mostab-core": "file:..",
    "uplot": "^1.6.32"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@tsconfig/svelte": "^5.0.8",
    "svelte": "^5.56.3",
    "svelte-check": "^4.6.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
