node_modules/
dist/
coverage/
.vite/
*.tsbuildinfo
*.log
.DS_Store

# playwright
test-results/
playwright-report/
blob-report/
__screens__/

# python
__pycache__/
*.pyc
.venv/

.claude/

# ltspice-mcp server config + run artifacts (working dir is this repo)
ltspice-mcp.toml
.ltspice-mcp/

# PDK gm/ID generation: fetched PDKs + staging, plus the generated tables themselves.
# The tables are large and fully reproducible with `tools/gen_gmid.py`, so they are not
# committed; the provenance notes + license text under data/pdk/ ARE tracked.
.pdk/
.pdkgen/
data/pdk/**/*.mostab.csv

# e2e demo fixture (regenerated from the EKV oracle by web/e2e/global-setup.ts)
web/e2e/fixtures/demo.mostab.csv

# ngspice init (ngbehavior=hsa so sectioned .lib works)
.spiceinit
